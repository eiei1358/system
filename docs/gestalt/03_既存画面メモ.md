# 既存画面メモ（チーム作成のHTML画面・スクリーンショット由来）

> 共有された画面イメージ（プレーンHTML）を、後続作業の参照用に記録したもの。
> 私（Claude）が作成した `prototype/admin/` のスタイル付きプロトタイプとは別系統の、
> シンプルな実装イメージ。**画面遷移と項目の確定情報**として扱う。

---

## UC703 カテゴリ管理

### 画面1：カテゴリ一覧
- 見出し：**カテゴリ一覧**
- リンク：**カテゴリ追加**
- 一覧テーブル（カテゴリID／カテゴリ名／操作）：

  | カテゴリID | カテゴリ名 | 操作 |
  |---|---|---|
  | CAT001 | 書籍 | 編集・削除 |
  | CAT002 | IT機器 | 編集・削除 |
  | CAT003 | 文具 | 編集・削除 |

### 画面2：カテゴリ登録
- 見出し：**カテゴリ登録**
- 入力項目：**カテゴリ名**（テキスト入力）
- ボタン：**確認** ／ **キャンセル**

### 画面3：登録内容確認
- 見出し：**登録内容確認**
- 確認文：「以下の内容でカテゴリを登録します。よろしいですか？」
- 表示：カテゴリ名 ＝ 資格参考書（例）
- ボタン：**登録する** ／ **キャンセル**

### 画面4：カテゴリ削除確認
- 見出し：**カテゴリ削除確認**
- 確認文：「以下のカテゴリを削除します。よろしいですか？」
- 表示テーブル：

  | 項目 | 値 |
  |---|---|
  | カテゴリID | CAT003 |
  | カテゴリ名 | 文具 |

- 注記：「※削除すると元に戻せません。」
- ボタン：**削除する** ／ **キャンセル**

> 遷移：一覧 →[カテゴリ追加]→ 登録 →[確認]→ 登録内容確認 →[登録する]→ 一覧
> 　　　一覧 →[削除]→ カテゴリ削除確認 →[削除する]→ 一覧
> ※編集も登録と同様（編集→確認→一覧）になる想定。

---

## UC704 検索キー管理

### 画面：検索キー管理
- 見出し：**検索キー管理**
- リンク：**検索キー追加**
- 検索：入力欄「ここに入力」＋ **検索** ボタン
- 一覧テーブル（ID／検索キー／操作）：

  | ID | 検索キー | 操作 |
  |---|---|---|
  | 001 | Java | 編集・削除 |

### 画面：検索キー編集
- 見出し：**検索キー編集**
- 入力項目：**検索キーID**（例：KEY001、編集不可想定）／**検索キー**（例：Java）
- ボタン：**保存** ／ **キャンセル**

### 画面：検索キー削除確認
- 見出し：**検索キー削除確認**
- 確認文：「以下の検索キーを削除します。よろしいですか？」
- 表示テーブル：

  | 項目 | 値 |
  |---|---|
  | 検索キーID | KEY003 |
  | 検索キー | PC |

- ボタン：**削除する** ／ **キャンセル**

> 遷移：管理（一覧）→[編集]→ 検索キー編集 →[保存]→ 一覧
> 　　　管理（一覧）→[削除]→ 検索キー削除確認 →[削除する]→ 一覧
> ⚠ **ID 表記ゆれ**：一覧画面では `001`、編集/削除確認では `KEY001`/`KEY003`。要統一。

---

## UC706 NGキーワード登録

### 画面：NGキーワード管理
- 見出し：**NGキーワード管理**
- セクション「NGキーワード登録」：入力欄 ＋ **登録** ボタン
- セクション「登録済みNGキーワード一覧」（選択／NGキーワード／登録日）：

  | 選択 | NGキーワード | 登録日 |
  |---|---|---|
  | ☐ | 暴言 | 2026/06/12 |
  | ☐ | 差別用語 | 2026/06/12 |
  | ☐ | 禁止ワード | 2026/06/12 |

- ボタン：**選択したキーワードを削除**（チェックボックスで複数選択→一括削除）
- リンク：**管理メニューへ戻る**

> 1画面に「登録フォーム＋一覧＋一括削除」を集約。削除は確認画面なしの一括方式。
> 検索キー管理（704）が個別の確認画面を持つのに対し、706 は一括削除でパターンが異なる。

---

## UC707 物品削除

### 画面1：物品削除確認
- 見出し：**物品削除確認** ／ セクション「削除対象物品」
- 表示テーブル：

  | 項目 | 値 |
  |---|---|
  | 商品ID | 1001 |
  | 商品名 | iPhone15 |
  | 出品者 | 山田太郎 |
  | 状態 | 出品中 |

- 確認文：「この物品を削除しますか？」
- ボタン：**削除する** ／ **キャンセル**

### 画面2：物品削除完了
- 見出し：**物品削除完了**
- 本文：「対象の物品を削除しました。」
- リンク：**物品一覧へ戻る** ／ **管理メニューへ戻る**

> 遷移：（物品一覧/監視結果から）→ 物品削除確認 →[削除する]→ 物品削除完了
> 完了画面は 708/709 と同じ「〜しました。＋一覧へ戻る／管理メニューへ戻る」型で統一されている。

---

## UC710 統計情報閲覧

### 画面1：統計情報閲覧（集計条件）
- 見出し：**統計情報閲覧** ／ セクション「集計条件」
- 入力項目：
  - **集計観点**：プルダウン（例：出品数 …）
  - **期間**：日付（`年/月/日`）〜 日付（`年/月/日`）
- ボタン：**集計する**
- リンク：**管理メニューへ戻る**

### 画面2：統計情報結果（集計結果）
- 見出し：**統計情報結果** ／ セクション「集計結果」
- 結果テーブル（項目／件数）：

  | 項目 | 件数 |
  |---|---|
  | 出品数 | 120件 |
  | 成約数 | 95件 |
  | 成約率 | 79% |

- セクション「グラフ表示」：画像プレースホルダ＋注記
  「※ここに棒グラフや折れ線グラフを表示」
- リンク：**条件を変更して再表示** ／ **管理メニューへ戻る**

> 遷移：集計条件 →[集計する]→ 集計結果 →[条件を変更して再表示]→ 集計条件

---

## UC709 利用制限

### 画面1：対象社員情報（利用制限の確認）
- 見出し：**対象社員情報**
- 表示テーブル：

  | 項目 | 値 |
  |---|---|
  | 社員番号 | 10001 |
  | 氏名 | 山田太郎 |
  | メールアドレス | yamada@example.com |
  | 現在の状態 | 利用中 |

- 確認文：「この社員の利用を制限しますか？」
- ボタン：**利用制限する** ／ **キャンセル**

### 画面2：利用制限完了
- 見出し：**利用制限完了**
- 本文：「対象社員の利用を制限しました。」
- リンク：**社員一覧へ戻る** ／ **管理メニューへ戻る**

> 遷移：（社員一覧から）→ 対象社員情報 →[利用制限する]→ 利用制限完了

---

## UC708 メッセージ削除

### 画面1：メッセージ削除確認
- 見出し：**メッセージ削除確認** ／ セクション「削除対象メッセージ」
- 表示テーブル：

  | 項目 | 値 |
  |---|---|
  | 送信者 | 山田太郎 |
  | 受信者 | 佐藤花子 |
  | 送信日時 | 2026/06/12 10:30 |
  | メッセージ内容 | ふざけるな |

- 確認文：「このメッセージを削除しますか？」
- ボタン：**削除する** ／ **キャンセル**

### 画面2：メッセージ削除完了
- 見出し：**メッセージ削除完了**
- 本文：「対象のメッセージを削除しました。」
- リンク：**メッセージ一覧へ戻る** ／ **管理メニューへ戻る**

> 遷移：（監視結果/メッセージ一覧から）→ メッセージ削除確認 →[削除する]→ メッセージ削除完了
> 物品削除（707）と同じ「削除対象を表で提示 → 確認 → 完了」の構成で統一されている。

---

## メモ・整合性の所感

- 全画面に共通で **「管理メニューへ戻る」** リンクがある（管理者メニュー=UC701 画面がハブ）。
- 完了画面のパターンが共通：「〇〇しました。」＋「一覧へ戻る」「管理メニューへ戻る」。
  708 だけでなく 707（物品削除）/709 完了も同じ型に揃えると統一感が出る。
- 利用制限の社員番号は **10001 形式**（数値5桁）。一方 `prototype/admin/index.html` の
  サンプルは **E2024001 形式**。**社員番号のフォーマットは要統一**。
- 統計の集計観点は要件書どおり「出品数／成約数／成約率／カテゴリ別／時系列」を網羅予定。
  グラフは現状プレースホルダ（実装時に棒/折れ線を描画）。
- これらはプレーンHTML（CSSなし）。デザインを揃える場合は `prototype/admin/` の
  共通スタイルへ寄せられる。
- **ID 形式が機能ごとにバラバラ**：社員=`10001`/`E2024001`、カテゴリ=`CAT001`、
  検索キー=`001`（一覧）/`KEY001`（編集）、物品=`1001`。命名規則を一覧化して統一しておくと、
  データモデル設計時に揺れが減る。検索キーは同一機能内でも表記が割れているので最優先で統一。
- **削除UIの方式が不統一**：703/704/707 は「個別の削除確認画面」、706 は「チェックボックス一括削除（確認なし）」。
  誤操作リスクを考えると、一括削除にも確認を入れるか方針を揃えたい。
- 確認画面のパターンが共通：「以下の内容で〜します。よろしいですか？」＋[実行][キャンセル]。
  削除確認には「※元に戻せません」注記あり。702/707/708/709 の確認画面もこの型に揃えると統一感が出る。
